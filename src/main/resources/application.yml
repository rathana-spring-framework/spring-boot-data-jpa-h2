server:
  port: 3334
spring:
  thymeleaf:
    mode: LEGACYHTML5
    cache: false
  h2:
    console:
      enabled: true

  datasource:
    data: classpath:/db/data.sql
    schema: classpath:/db/schema.sql
